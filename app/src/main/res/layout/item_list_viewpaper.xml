<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.experiment.npe.ui.main.vm.ViewPagerItemListViewModel" />

        <variable
            name="list_viewModel"
            type="com.experiment.npe.ui.main.vm.ViewPagerItemListViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:binding="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:selectableItemBackground"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="10dp"
        binding:onClickCommand="@{list_viewModel.itemClick}"
        binding:onLongClickCommand="@{list_viewModel.itemLongClick}">

        <ImageView
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@{list_viewModel.drawableImg}"
            binding:url="@{list_viewModel.entity.img}" />

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="6dp"
            android:text="@{list_viewModel.entity.id == -1 ? list_viewModel.getPosition() + list_viewModel.entity.name : list_viewModel.entity.name}" />
    </LinearLayout>
</layout>
